# Supabase Configuration (Production)
NEXT_PUBLIC_SUPABASE_URL=https://your-production-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-production-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-production-service-role-key
SUPABASE_JWT_SECRET=your-production-jwt-secret

# App Configuration (Production)
NEXT_PUBLIC_APP_NAME="Prato Rinaldo - Piattaforma Digitale"
NEXT_PUBLIC_APP_URL=https://pratorinaldo.it
NEXT_PUBLIC_TENANT_SLUG=prato-rinaldo

# Production Services
RESEND_API_KEY=your-production-resend-key
SENTRY_DSN=your-production-sentry-dsn
NEXT_PUBLIC_POSTHOG_KEY=your-production-posthog-key

# Docker/Deployment
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0


# =============================================================================
# SUPABASE EDGE FUNCTIONS (Managed via Supabase Secrets)
# =============================================================================
#
# IMPORTANT: These variables are NOT stored in .env files!
# They are managed via Supabase Secrets using the CLI:
#
# Setup secrets:
#   supabase secrets set RESEND_API_KEY=re_your_key
#   supabase secrets set WEBHOOK_SECRET=$(openssl rand -base64 32)
#   supabase secrets set APP_URL=https://pratorinaldo.it
#
# View secrets:
#   supabase secrets list
#
# Edge Functions automatically have access to these secrets at runtime.
# =============================================================================

# Email Notifications Edge Function
# Get your API key from https://resend.com (free tier: 100 emails/day)
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Webhook Security (auto-generate with: openssl rand -base64 32)
# WEBHOOK_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Application URL (used in email templates for links)
# APP_URL=https://pratorinaldo.it

# =============================================================================
# SUPABASE EDGE FUNCTIONS - CRON SCHEDULES
# =============================================================================
#
# These are configured via Supabase Dashboard > Database > Cron Jobs
# See DEPLOYMENT.md for complete setup instructions
#
# - calculate-badges:  0 * * * *      (every hour)
# - cleanup-sessions:  0 2 * * *      (daily at 2 AM)
# - aggregate-stats:   0 */6 * * *    (every 6 hours)
#
# Monitoring:
#   SELECT * FROM cron.job ORDER BY jobname;
#   SELECT * FROM cron.job_run_details ORDER BY start_time DESC LIMIT 10;
# =============================================================================
